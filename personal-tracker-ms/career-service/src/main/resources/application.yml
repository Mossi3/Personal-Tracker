server:
  port: 8084

spring:
  application:
    name: career-service
  datasource:
    url: jdbc:h2:mem:careerdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true